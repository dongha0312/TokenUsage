# TokenUsage 개발 기록

메뉴바에 Claude·Codex·Gemini 구독 사용량을 띄우는 맥 앱을 만든 과정. 무엇을 만들었는지보다
**무엇이 틀렸고 어떻게 알았는지**를 남긴다. 이 앱에서 값나가는 부분은 전부 "맞다고 생각한 것이
실제로는 틀렸더라"에서 나왔다.

---

## 1. 출발점: 데이터가 어디 있는지부터가 문제였다

요청은 "세 구독의 남은 양과 리셋 시각을 메뉴바에". 구현보다 **출처 확보**가 전부였다.

처음 조사에서 세 곳의 사정이 전혀 달랐다.

| | 최초 판단 |
|---|---|
| **Codex** | `~/.codex/sessions/**/*.jsonl` 에 `rate_limits` 가 그대로. 공식 수치, 추정 불필요 |
| **Claude** | JSONL 에 토큰은 전부 있으나 **한도를 공개하지 않음** |
| **Gemini** | 로컬에 사용량 데이터가 **아예 없음** |

Gemini는 "데이터가 없으니 v1에서 빼자"고 제안했다. → **틀렸다.** 사용자가 "Gemini 앱 설정에
사용량 한도 화면이 있다"고 알려줬고, 확인해보니 정말 있었다. 없다고 단정한 건 로컬 파일만
뒤졌기 때문이다.

Antigravity도 조사했다. 데스크톱 앱은 `state.vscdb` 에 크레딧을 캐시하지만 **앱이 실행 중일
때만 갱신**되고, CLI는 사용량을 아예 안 남긴다. "켜야 갱신되는 값을 메뉴바에 거는 건 의미가
없다"고 판단해 제외했다. 이 판단은 지금도 유효하다.

---

## 2. Claude: 역산 → 캐시 → 실시간, 두 번 갈아엎었다

### 2-1. 429 역산 (폐기)

Anthropic이 플랜별 한도를 공개하지 않으니 로그에서 역산하기로 했다. JSONL 에 실제로
429 로 거절당한 기록(`quotaLimits.rateLimitType == "five_hour"`)이 남는다. 막힌 시점의
5시간 블록 토큰 합계가 곧 그 계정의 한도다.

구현: 블록 분할, 중앙값 보정(최대값은 한도를 과대평가해 "여유 있다"고 거짓말한다),
mtime 캐시 스캐너(7일치 71개 파일 324MB → 첫 스캔 7.5초, 이후 0.02초).

**결과는 틀렸다.** 사용자가 `/usage` 실물을 보여줬을 때:

```
역산:  세션 XX% 사용
실제:  세션 YY% 사용   ← 역산이 실제의 약 2배
```

한도를 절반으로 잡았고, 5시간 창 시작점(정시 내림)도 주간 앵커도 틀렸다.
**실측 대조가 없었으면 계속 틀린 숫자를 보여줬을 것이다.**

### 2-2. `~/.claude.json` 캐시 (중간 단계)

`/usage` 를 돌린 직후 `~/.claude.json` 이 갱신된 걸 보고 뒤져서 `cachedUsageUtilization` 을
찾았다. 서버가 내려준 수치가 그대로 있었다.

```json
"limits": [
  { "kind": "session",       "percent": <n>, "resets_at": "..." },
  { "kind": "weekly_all",    "percent": <n>, "resets_at": "..." },
  { "kind": "weekly_scoped", "percent": <n>, "scope": { "model": { "display_name": "..." } } }
]
```

역산 기계 전체(블록 분할·한도 보정·롤링 창·324MB 스캐너)를 **파일 하나 읽는 것**으로
대체했다. ClaudeReader 290줄 → 127줄, 테스트 22초 → 0.05초. 그리고 맞는 값이 나왔다.

### 2-3. 실시간 웹 (최종)

하지만 실시간이 아니었다. **62분간 33회 샘플링**하는 동안 Claude Code가 계속 API를
호출했는데도 `fetchedAtMs` 가 한 번도 안 움직였다. 설정 백업(`~/.claude/backups/`)을
되짚으니 `cachedUsageUtilization` **키 자체가 `/usage` 실행 순간에 처음 생겼다.**
일반 사용으로도, 세션 시작으로도 갱신되지 않는다.

그래서 Gemini에 쓰던 방식을 Claude에도 적용했다 — `claude.ai/settings/usage` 를 숨은
WKWebView로 읽는다. 같은 시점에 두 값이 달랐다 — **웹이 더 크고, 캐시는 뒤처져 있었다.** 웹이 살아 있는 값이다.

로컬 캐시는 폴백으로 남겼다. 로그인 전에도 뭔가는 보여주고, "로컬 값 표시 중"이라고 밝힌다.

---

## 3. 검증이 잡아낸 버그들

단위 테스트는 내가 만든 샘플만 검증한다. **실제 데이터·실제 화면과 대조했을 때만** 드러난
것들이다.

### 굳은 수치를 현재값처럼 보여줌
Codex 5시간 창이 `N% 사용, 리셋 HH:MM UTC` 인데 그 시각이 이미 지나 있었다.
로그의 `rate_limits` 는 마지막 실행 시점의 스냅샷이라 안 쓰면 숫자가 굳는다.
→ `resets_at` 이 지났으면 그 창은 비워진 것으로 처리(0%). 다음 리셋 시각은 일부러 비운다 —
창은 다음 사용 시점부터 시작하므로 안 쓰는 동안은 예정된 리셋이 없다.

### 주간 리셋이 `76:53`
시:분 형식을 76시간에 그대로 적용했다. → 하루 넘으면 일 단위.

### 앱이 떠 있기만 하고 아무것도 안 함
`MenuBarExtra` 의 `label:` 에 붙인 `.task` 가 **호출되지 않는다.** 그 자리는 일반 뷰
생명주기를 타지 않는다. 프로세스가 살아있는 것만 보고 "동작한다"고 판단한 게 잘못이었다.
메뉴바 앱은 stdout도 없고 NSLog도 `log show` 에 안 잡혀서 눈에 보이지도 않았다.
→ `UsageModel.init()` 에서 시작. 그리고 `TOKENUSAGE_DEBUG=1` 파일 로깅을 상설화했다.

### 로그인 항목 등록을 건너뜀
가드가 `status == .notRegistered` 일 때만 등록을 시도했는데 실제 상태는 `.notFound` 였다.
→ `.enabled` 가 아니면 무조건 시도.

### 로그인 버튼이 뜨자마자 사라짐
`refreshWeb()` 이 "로그인 필요" 안내를 붙여도 10초마다 도는 `refreshLocal()` 이
캐시 값으로 덮어써서 지웠다. 사용자가 "로그인 어디서 해?"라고 물어보고서야 알았다.
→ 실패 사유를 따로 보관 + 하단에 **항상 보이는** 로그인 링크.

### 웹 조회 한 사이클 92초
로그인 안 된 상태에서 세 곳이 각각 30초 타임아웃을 다 태웠다. 원인이 제공자마다 달랐다:
claude.ai 는 `/login` 으로 **리다이렉트**하지만 chatgpt.com·gemini.google.com 은
**같은 URL에서 로그인 화면을 그려서** URL로는 못 잡는다. → 내용으로도 판별(6초 유예).
**92초 → 15초.**

### SPA 렌더 전에 성급히 파싱
"Usage limits" 제목만 뜨면 다 그려진 걸로 보고 파싱해서 빈손으로 돌아왔다.
그리고 그걸 "페이지 구조가 바뀐 듯"이라고 **잘못 보고**했다.
→ 숫자가 실제로 파싱될 때까지 대기.

### 축약 요일을 못 읽음
claude.ai 문구가 `오후 9:10에 재설정` → `재설정: (목) 오전 2:10` 으로 바뀌었는데
파서는 `목요일` 만 찾았다. 그때는 우연히 맞았지만 다른 요일이면 며칠씩 틀린다.
→ 괄호로 감싼 축약형만 추가 인식(한 글자를 아무 데서나 찾으면 "토큰"의 "토"를 토요일로 읽는다).

### Gemini를 "5시간 한도"라고 표시
창 종류를 뭉뚱그린 탓. Gemini 창은 5시간이 아니다(한 시간도 안 남은 상태로 관측됐다).
Codex 주간은 "주간 · 모든 모델"로 나왔는데 이건 Claude 전용 표현이다.
→ `session(hours: Int?)` 로 분리. 길이를 아는 벤더만 시간을 싣는다.

### 실패한 제공자에 "방금 업데이트됨"
데이터가 없는데 방금 갱신된 것처럼 보였다. → 실패에는 갱신 시각을 달지 않는다.

### 10초마다 불필요한 디스크 읽기
세 곳 다 실시간으로 받고 있어도 `~/.codex/sessions` 전체를 계속 훑었다.
→ 실시간 값이 있는 제공자는 건너뛴다.

### 로그인 중에 예약 갱신이 화면을 가로챔
로그인 창을 열어둔 상태에서 5분 주기 갱신이 돌면, 같은 웹뷰를 재사용하면서 페이지를
다시 로드해버린다. **비밀번호나 인증 코드를 입력하는 중이면 처음부터 다시 해야 한다.**
→ 로그인 창이 열려 있으면 페이지를 갈아끼우지 않고, 현재 화면만 짧게(3초) 들여다본다.
(로그인을 막 마쳤으면 이미 사용량 화면일 수 있으므로 그 경우는 바로 잡힌다.)

### 아이콘 생성기가 규격에 없는 이름을 만듦
`icon_32x32@2x`(64px)가 빠지고 대신 규격에 없는 `icon_64x64@2x`가 들어갔다.
"크기의 절반에 @2x를 붙인다"는 규칙을 유추한 탓.
→ (이름, 픽셀) 쌍을 명시적으로 나열한다.

### 아이콘이 전부 2배로 렌더돼 16pt 슬롯이 비어 있었다
`NSImage.lockFocus()` 는 **화면의 backing scale** 로 그린다. 레티나에서 돌리니
`icon_16x16.png` 가 실제로는 32×32px 였고, `iconutil` 은 파일명이 아니라 **실제 픽셀**로
분류하므로 모든 슬롯이 한 단계씩 밀렸다. 결과적으로 icns 에 16pt 칸(`ic04`·`ic11`)이
아예 없어서 macOS 가 32pt 그림을 축소해 썼다 — 이 파일 주석이 "축소가 아니라 직접 그려서
안 뭉개진다"고 주장하던 바로 그 실패다. 1배 화면에서 돌리면 결과까지 달라졌다.
`rep.size = ...` 는 포인트 크기 메타데이터라 이걸 막지 못한다(무의미한 한 줄이었다).
→ 픽셀 수를 명시한 `NSBitmapImageRep` 에 직접 그린다. 슬롯 6개 → 10개.

### 셸 우선순위 때문에 아이콘 변환이 항상 실행됨
`[ -f x ] || A && B` 를 "없으면 A 하고 B" 로 읽었는데, `||` 와 `&&` 는 우선순위가 같고
왼쪽 결합이라 `([ -f x ] || A) && B` 로 묶인다. 파일이 있어도 B(변환)가 매번 돌았고,
정작 디자인을 고쳐도 A(생성)는 영원히 안 돌아 스크립트가 사실상 죽은 코드였다.
게다가 오류를 전부 `|| true` 로 삼켜서, 아이콘 없이 빌드해도 "완료"라고 찍혔다.
→ `if` 문으로 명시하고, 실패는 삼키지 않는다.

---

## 4. 알림: 가장 오래 헤맨 곳

`UNUserNotificationCenter` 가 계속 `Notifications are not allowed for this application`.
**두 번 틀린 원인을 말했다.**

1. "ad-hoc 서명이라 그렇다, 서명하면 된다" → Apple Development 인증서로 서명해도 실패
2. "Developer ID 서명이 필요하다" → Developer ID 발급해서 서명해도 실패

배제한 것: ad-hoc / Apple Development / Developer ID 서명, 새 번들 ID, `lsregister -f`,
`LSUIElement` on/off, 기동 후 지연 요청, 플랫폼·SDK 메타데이터, 앱 아이콘, 시스템 전체 차단
(다른 앱 113개는 정상 등록돼 있었다).

**실제 원인은 두 조건이 겹친 것이었다.**

최소 테스트 앱(`NotifyTest`)을 Xcode로 만들어 변수를 하나씩 분리하고서야 보였다:

- `/private/tmp` 의 Xcode 빌드 → 즉시 실패
- `/Applications` 의 Xcode 빌드 → **권한 팝업이 떴다**
- `/Applications` 의 손조립 번들 → 즉시 실패

즉 **Xcode 빌드**여야 하고, 또 **번들 ID에 실패 이력이 없어야** 한다. macOS는 거부된
식별자를 기억하고 재서명으로는 안 지워진다. `com.dongha.tokenusage` 는 초기 미서명 상태로
수십 번 실행되며 오염돼 있었다.

→ `.xcodeproj` 를 만들고(UsageCore 는 로컬 SwiftPM 패키지로 참조) 번들 ID를
새 식별자로 바꾸니 권한 팝업이 떴고, 허용 후 알림이 동작했다.

**부작용:** `WKWebsiteDataStore.default()` 가 번들 ID별로 분리돼 있어 로그인이 전부 풀렸다.
`~/Library/WebKit/<번들ID>/` 와 `~/Library/HTTPStorages/<번들ID>.binarycookies` 를
옮겨서 복구했다.

알림이 안 되는 빌드를 위해 **메뉴바 경고 글리프**(80%/95%)를 넣어뒀다. 권한이 필요 없어서
어떤 빌드에서도 동작하고, 메뉴바는 항상 보이니 실용적으로 1차 신호 역할을 한다.

---

## 5. 설계에서 지킨 것

### 자격증명을 만지지 않는다
Keychain에서 OAuth 토큰을 꺼내 비공개 API를 부르면 더 가볍다. 실제로 유사 앱들이 그렇게
한다. 하지만 **항상 떠 있는 앱이 계정 자격증명을 계속 만지는 것**을 피했다.
웹뷰가 자기 쿠키만 갖고, 사용자가 앱 안에서 한 번 로그인한다.
(조사 중 자격증명 탐색이 도구 차원에서 차단된 적이 있는데, 그 차단이 옳았다고 본다.)

### 굳은 값을 현재값처럼 보여주지 않는다
세 출처 모두 "어느 시점의 스냅샷"이라는 성질이 있다. 그래서:
- 리셋이 지난 창은 비운다
- 제공자마다 **언제 기준인지** 항상 표시한다
- 웹 실패 시 로컬로 떨어지되 "로컬 값 표시 중"이라고 밝힌다

### 모르는 분모를 지어내지 않는다
한도를 모르면 `usedPercent` 를 `nil` 로 둔다. 추측한 숫자로 막대를 그리면 그럴듯한
거짓말이 된다.

### 파서는 문구가 아니라 의미를 넘긴다
`WindowKind.session(hours:)` / `.weeklyScoped("Fable")`. 파서가 "5시간 한도" 같은 완성된
문자열을 만들면 그 언어에 갇힌다. 벤더 페이지는 **계정** 언어를, UI는 **시스템** 언어를
따라야 하므로 분리했다. 한국어·영어 UI가 이 구조 덕에 동작한다.
모르는 종류는 버리지 않고 그대로 통과시킨다 — 벤더가 새 한도를 추가해도 표시된다.

### 날짜 형식 하나에 기능 전체를 걸지 않는다
퍼센트는 숫자라 언어를 안 타지만 리셋 시각은 `2026. 9. 19. 오후 9:57` 처럼 지역 형식이다.
웹에서 날짜를 못 읽으면 로컬 로그의 epoch로 채운다.

### 사실대로만 라벨을 붙인다
Gemini는 창 길이를 공개하지 않으므로 `session(hours: nil)` → "현재 한도".
5를 넣어두면 거짓말이 된다.

---

## 6. 최종 구성

```
Sources/UsageCore/          순수 로직. 테스트 대상
  Usage.swift               공통 모델 (WindowKind · UsageWindow · ProviderUsage)
  L10n.swift                한국어·영어 문자열
  ClaudeReader.swift        ~/.claude.json 의 cachedUsageUtilization (폴백)
  ClaudeWebParse.swift      claude.ai 텍스트 → 수치
  CodexReader.swift         ~/.codex/sessions 의 rate_limits (폴백)
  CodexWebParse.swift       chatgpt.com 텍스트 → 수치 (남은 비율을 사용률로 뒤집음)
  GeminiParse.swift         gemini.google.com 텍스트 → 수치
Sources/TokenUsage/         앱. AppKit/WebKit 의존
  WebUsageReader.swift      숨은 WKWebView (셋 공용, 조회할 때만 생성 후 해제)
  ProviderIcon.swift        설치된 앱에서 아이콘 조회 (NSWorkspace)
  LoginItem.swift           SMAppService 로그인 항목
  LimitNotifier.swift       한도 임박 알림
  UsageModel.swift          갱신 스케줄 · 실시간/폴백 결정
  PanelView.swift           드롭다운
```

| | 출처 | 주기 |
|---|---|---|
| Claude | claude.ai 웹뷰 → `~/.claude.json` 폴백 | 5분 / 10초 |
| Codex | chatgpt.com 웹뷰 → 세션 로그 폴백 | 5분 / 10초 |
| Gemini | gemini.google.com 웹뷰 (폴백 없음) | 5분 |

빌드는 Xcode 경로가 기본(`./build.sh --install`), `NO_XCODE=1` 로 SwiftPM 조립도 가능하다
(알림만 빠진다). 외부 의존성 0개, 테스트 80개.

---

## 7. 남은 한계

- **웹 파싱은 렌더된 텍스트를 읽는다.** 벤더가 페이지를 바꾸면 깨진다. 깨지면
  "수치를 못 읽음"이 뜨고 로컬 폴백으로 떨어진다. claude.ai 파서는 한국어·영어를 받고,
  Gemini는 `hl=en` 으로 못박았다. chatgpt.com 영어 날짜 형식은 검증하지 못했지만,
  리셋 시각을 로컬에서 채우는 경로가 있어 치명적이지 않다.
- **Gemini는 폴백이 없다.** 로컬에 아무것도 없어서 로그인 전에는 빈다.
- **Codex 로컬 폴백은 이 맥의 세션만 반영한다.** 웹 경로가 있는 이유가 그것이다.
  단일 기기 계정에서는 로컬과 웹이 정확히 일치하는 걸 확인했다(테스트로 회귀 방지).
- **구글 로그인이 웹뷰에서 막힐 수 있다.** 구글이 임베디드 웹뷰 OAuth를 거부한다.
  이메일 코드 로그인을 쓰면 된다.
- **메모리 ~100MB.** WKWebView 를 한 번 쓰면 WebKit 이 프로세스에 로드된 채 남는다.
  웹뷰 자체는 조회할 때만 만들고 버려서 유휴 시 보조 프로세스는 0개다.
- **알림은 Xcode 빌드 + 깨끗한 번들 ID 조합에서만 된다.** 위 4절 참고.

---

## 8. 아이콘

`Xcode/make-icon.swift` 가 AppKit 으로 직접 그린다. 이미지 파일 대신 스크립트를 두면 디자인을
고칠 때 한 곳만 보면 되고, 각 크기를 축소가 아니라 **직접 그려서** 16pt 에서도 막대가 뭉개지지
않는다.

모티프는 패널의 사용량 막대 그대로다. 세 줄은 세 서비스를, 채움 정도는 사용량을 뜻하고,
맨 아래만 경고색인 건 "한도에 가까운 하나를 메뉴바에 띄운다"는 앱의 동작을 그대로 보여준다.

---

## 9. 내가 못 한 것

- **로그인.** 비밀번호 입력은 하지 않는 선이다. 사용자가 직접 했다.
- **저장소 공개.** LICENSE·README는 준비했지만 푸시는 하지 않았다.
  현재 `token_usage/` 가 부모 `ios-Dev` 저장소 안에 있어 별도 저장소로 떼낼지 판단이 필요하다.
- **chatgpt.com 영어 페이지 검증.** 계정 언어가 한국어라 영어 경로는 코드상 대비만 해뒀다.

---

## 10. 돌아보며

이 작업에서 반복된 패턴 하나: **"동작한다"고 판단한 근거가 약했다.**

- 프로세스가 살아있다 → 앱이 동작한다 (아니었다, `.task` 가 안 불렸다)
- 로그에 성공이라고 찍힌다 → 수치가 맞다 (대조 안 했었다)
- 서명했다 → 알림이 될 것이다 (두 번 틀렸다)

실제로 값을 만들어낸 건 전부 대조였다. `/usage` 화면과 앱 숫자를 맞춰보고, 웹 페이지와
로컬 로그를 맞춰보고, 최소 테스트 앱으로 변수를 하나씩 분리하고. `RealDataTests` 를
남겨둔 것도 같은 이유다 — 내가 만든 샘플이 아니라 진짜 파일로 돌려야 포맷 변화가 잡힌다.
